query GetProducts {
    products {
        id
        name
        imageUrl
    }
}

query GetProductById($productId: Uuid!) {
    product(id: $productId) {
            id
            name
            description
            quantity
            imageUrl
        }
}